name: Verify stream stability

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/verify-stream-stability.yml

permissions:
  contents: write

jobs:
  windows-build:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run and capture build
        shell: pwsh
        run: |
          npm run build 2>&1 | Tee-Object "build-full.log"
          $code = $LASTEXITCODE
          "Exit code: $code" | Set-Content ".github/stream-stability-build.txt"
          Get-Content "build-full.log" -Tail 220 | Add-Content ".github/stream-stability-build.txt"
          exit 0
      - name: Publish build result and remove workflow
        shell: pwsh
        run: |
          Remove-Item ".github/workflows/verify-stream-stability.yml" -Force
          git config user.name "James"
          git config user.email "257743569+jamesjesse24@users.noreply.github.com"
          git add .github/stream-stability-build.txt .github/workflows/verify-stream-stability.yml
          git commit -m "Capture stream stability build result"
          git push origin HEAD:main
